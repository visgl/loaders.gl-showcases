import { render } from "@testing-library/react";
import { Dashboard } from "./index";

// TODO decide which kind of tests can be used here.
// We don't have gl context in node.js and we can't use render method from @testing-library/react mosule.
test("Renders dashboard without crashing", () => {
  // render(<Dashboard />);
});
